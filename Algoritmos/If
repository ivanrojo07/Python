x=int(raw_input("Escribe el valor de x: "))
#raw input es entrada de un valor en crudo y al utilizar int
#al principio convertimos esa entrada en un entero

if (x%2 ==0):
	print "el numero es par"
else:
	print "el numero es impar"
